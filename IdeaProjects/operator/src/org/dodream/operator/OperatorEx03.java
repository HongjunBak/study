package org.dodream.operator;

public class OperatorEx03 {
    public static void main(String[] args) {
        int number = 85;
        String grade = (number >= 100) ? "숫자가 100 이상이다." : "숫자가 100 미만이다.";
        System.out.println(grade);
    }
}
